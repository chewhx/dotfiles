- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/GitHub
    - ~/Dev

- shell:
    -
        command: ./setup_homebrew.zsh
        stdout: true
        stderr: true
